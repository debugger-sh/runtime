[package]
name = "wasm-instrument"
version = "0.1.0"
edition = "2024"

[dependencies]
gimli = "0.33.0"
object = { version = "0.38", default-features = false, features = ["read", "wasm"] }
wasm-encoder = { version = "0.245", features = ["wasmparser"] }
wasmparser = "0.245"
